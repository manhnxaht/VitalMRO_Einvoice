<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <!-- Tree view for Debit Notes -->
  <record id="view_account_move_credit_note_list" model="ir.ui.view">
    <field name="name">account.move.credit.note.list</field>
    <field name="model">account.move</field>
    <field name="arch" type="xml">
      <tree string="Credit Notes">
        <field name="name"/>
        <field name="partner_id"/>
        <field name="invoice_date"/>
        <field name="amount_total"/>
        <field name="state"/>
      </tree>
    </field>
  </record>

  <!-- Action filtering on type_bill -->
  <record id="action_account_move_credit_note" model="ir.actions.act_window">
    <field name="name">Debit Notes</field>
    <field name="res_model">account.move</field>
    <field name="view_mode">list,form</field>
    <field name="view_id" ref="view_account_move_credit_note_list"/>
    <field name="domain">[('e_invoice_type_code','=','02')]</field>
  </record>

  <!-- Menu under Accounting → Customers -->
  <menuitem
    id="menu_account_move_credit_note"
    name="Credit Notes"
    parent="account.menu_finance_receivables"
    action="action_account_move_credit_note"
    sequence="5"/>
</odoo>
